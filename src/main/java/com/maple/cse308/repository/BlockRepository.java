/*
package com.maple.cse308.repository;

import com.maple.cse308.entity.Block;
import com.maple.cse308.entity.BlockIdentity;
import org.springframework.data.repository.CrudRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface BlockRepository extends CrudRepository<Block, BlockIdentity> {
    Block save(BlockIdentity blockId);
    void delete(BlockIdentity blockId);
    boolean existsByBlockIdentity(BlockIdentity blockId);
    Block findByBlockIdentity(BlockIdentity blockid);
    List<Block> findByBlockingIdBlockerId(Integer blockerId);

}
*/
